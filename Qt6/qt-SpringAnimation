`` 是 QML 中一种**弹簧物理模型动画**。它模拟一个物体受到弹簧力和阻尼力作用后的运动效果，不是简单的线性插值。

例如：

```qml
Behavior on scale {
    SpringAnimation {
        spring: 5
        damping: 0.3
    }
}
```

表示：

> 当 `scale` 属性变化时，不直接移动到目标值，而像一个连接弹簧一样，先超过目标位置（可能），然后振荡几次，最后稳定下来。

------

## 1. 与 NumberAnimation 的区别

普通动画：

```qml
Behavior on scale {
    NumberAnimation {
        duration: 200
    }
}
```

效果：

```
1.0
 |
 |\
 | \
 |  \
 |   \
0.94  ----------------
```

直接平滑到目标值。

SpringAnimation：

```
1.0
 |
 |\
 | \
 |  \__
 |     \____
 |          \__
0.94           ----
```

类似：

- 按下按钮
- 松手
- 按钮弹回来

------

## 2. spring 参数

```qml
spring: 5
```

表示弹簧强度。

范围通常：

- 小值：软弹簧，动作慢
- 大值：硬弹簧，动作快

例如：

```qml
spring: 1
```

效果：

```
慢慢回弹
spring: 20
```

效果：

```
快速弹回
```

------

## 3. damping 参数

```qml
damping: 0.3
```

表示阻尼系数，也就是摩擦力。

控制振荡次数。

### damping 小

例如：

```qml
damping: 0.1
```

效果：

```
目标值
  |
  |   /\      /\
  |  /  \    /  \
--+-------\/------\----
```

弹很多次。

------

### damping 大

例如：

```qml
damping: 0.8
```

效果：

```
目标值
  |
  |\
  | \
--+--\-------------
```

几乎没有回弹。

------

## 4. 你的按钮效果

你的代码：

```qml
scale: wowButton.down ? 0.94 : 1.0

Behavior on scale {
    SpringAnimation {
        spring: 5
        damping: 0.3
    }
}
```

流程：

鼠标按下：

```
scale:

1.0
 |
 |
0.94
```

立即压缩。

鼠标释放：

```
0.94
 |
 |    弹簧恢复
 |
1.0
```

可能：

```
0.94
 ↓
1.05
 ↓
0.98
 ↓
1.01
 ↓
1.0
```

产生类似真实按钮回弹。

------

## 5. SpringAnimation 常用于什么？

典型场景：

### 按钮点击

```qml
scale: pressed ? 0.95 : 1
```

------

### 拖拽物体归位

例如：

```qml
x: targetX

Behavior on x {
    SpringAnimation {}
}
```

松手后回弹。

------

### UI 卡片出现

例如：

```qml
y: visible ? 0 : 500
```

配合 SpringAnimation：

```
500
 |
 |
 0
```

有弹入效果。

------

## 6. SpringAnimation 和 SmoothedAnimation 区别

Qt QML 中还有：

```qml
SmoothedAnimation
```

它更像机械缓动：

```
慢 -> 快 -> 慢
```

而：

```qml
SpringAnimation
```

是真实物理弹簧：

```
快 -> 超调 -> 振荡 -> 稳定
```

------

对于你的按钮，`SpringAnimation` 是比较合适的选择，因为现代 UI 的点击反馈通常希望有一点物理回弹感。参数：

```qml
spring: 5
damping: 0.3
```

属于明显回弹效果。如果想更接近 iOS / Material Design 的按钮反馈，可以提高阻尼，例如：

```qml
SpringAnimation {
    spring: 4
    damping: 0.7
}
```

回弹会更克制。