## **_set_se_translator**

 是 MSVC 提供的一个非常实用的 CRT 函数，它的作用是：

**将 Windows 的 Structured Exception Handling (SEH) 异常（如空指针访问、除零、栈溢出等硬件异常）翻译成 C++ 异常**，

这样你就可以用普通的 try { } catch (...) 或 catch (const std::exception&) 等 C++ 异常机制来统一处理它们。